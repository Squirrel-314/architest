// Here I will put what materials work well against which disasters

var danger = {
   flood: "flood",
}

let flood
let mud
let fire
let material;
let danger = flood;

if (material == resouces.mud) {
   if (danger = flood) {
      if (Math.random() < 0.9) {
         destroy();
      }
   }
   if (danger = fire) {
      if (Math.random() < 0.05) {
         destroy();
      }
   }
}

//Like limestone, sandstone is a sedimentary rock, formed by the settling and compression of materials over time. ... While limestone's density makes it strong under heavy pressure, sandstone is harder and provides better resistance against heat and impact

function destory() {
   alert("Your building was flimsy and destroyed. Get an education (and a new job)");
}
